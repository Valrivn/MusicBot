# =============================================================================
# Voxaria MusicBot - Production Environment Template
# =============================================================================
# COPY THIS FILE TO .env AND FILL IN ALL VALUES BEFORE DEPLOYING
# NEVER COMMIT THE ACTUAL .env FILE WITH REAL SECRETS
# =============================================================================

# -----------------------------------------------------------------------------
# Discord Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Bot token from Discord Developer Portal > Bot > Reset Token
DISCORD_BOT_TOKEN=
# Application ID from Discord Developer Portal > General Information
DISCORD_CLIENT_ID=
# Client Secret from Discord Developer Portal > General Information > Reset Secret
DISCORD_CLIENT_SECRET=
# Your Discord User ID (right-click username > Copy ID) - OPTIONAL, only for owner-exclusive commands
DISCORD_OWNER_ID=
# Guild ID for development/testing (optional, leave empty for global commands)
DISCORD_GUILD_ID=
# Website-only mode (default true): slash commands (e.g. /play) are NOT deployed.
# Playback is controlled exclusively through the web dashboard (voxaria.lovable.app).
# Set to 'true' to re-enable Discord slash commands.
DISCORD_COMMANDS_ENABLED=false

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3002
NODE_ENV=production
# Production frontend URL (e.g., https://voxaria.lovable.app)
FRONTEND_URL=https://voxaria.lovable.app

# -----------------------------------------------------------------------------
# Redis Configuration (only required for karaoke queues)
# -----------------------------------------------------------------------------
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DATABASE_PATH=./voxaria.db

# -----------------------------------------------------------------------------
# JWT Configuration (keys auto-generated in ./keys/)
# -----------------------------------------------------------------------------
JWT_PRIVATE_KEY_PATH=./keys/private.pem
JWT_PUBLIC_KEY_PATH=./keys/public.pem
JWT_ISSUER=voxaria-bot
JWT_AUDIENCE=voxaria-web
JWT_ACCESS_EXPIRY=15
JWT_REFRESH_EXPIRY=7

# -----------------------------------------------------------------------------
# Optional 3rd-party APIs - NONE of these are required.
# Music search/playback uses yt-dlp (no key). Spotify links are resolved to
# YouTube via yt-dlp (no key). Lyrics come from LRCLIB (https://lrclib.net),
# which needs no API key. Genius/Spotify keys only add optional extras.
# -----------------------------------------------------------------------------
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# GENIUS_CLIENT_ID=
# GENIUS_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# YouTube / Invidious / Piped Configuration
# -----------------------------------------------------------------------------
# YOUTUBE_API_KEY=
INVIDIOUS_INSTANCES=https://yewtu.be,https://invidious.snopyta.org,https://invidious.nerdvpn.de
PIPED_INSTANCES=https://piped.api.kavin.rocks,https://piped.mha.fi,https://piped.sorcerer.city

# -----------------------------------------------------------------------------
# YouTube Cookie Settings
# -----------------------------------------------------------------------------
# COOKIES_FROM_BROWSER=chrome
COOKIES_FILE=./cookies.txt

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Karaoke Configuration
# -----------------------------------------------------------------------------
DEMUCS_MODEL=htdemucs
KARAOKE_CONCURRENCY=2
KARAOKE_OUTPUT_DIR=./audio_cache/karaoke

# -----------------------------------------------------------------------------
# Bot Settings
# -----------------------------------------------------------------------------
EMBED_COLOR=#FF6B6B
SUPPORT_SERVER=https://discord.gg/your_support_server
WEBSITE=https://voxaria.lovable.app
STATUS=🎵 Voxaria | /play

# -----------------------------------------------------------------------------
# Sharding Settings
# -----------------------------------------------------------------------------
TOTAL_SHARDS=auto
SHARD_LIST=auto
SHARD_MODE=process
SHARD_RESPAWN=true
SHARD_SPAWN_DELAY=5500
SHARD_SPAWN_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Session Restore Settings
# -----------------------------------------------------------------------------
SESSION_RESTORE_ENABLED=false

# -----------------------------------------------------------------------------
# Rate Limiting Settings
# -----------------------------------------------------------------------------
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX=100
RATE_LIMIT_BURST=20